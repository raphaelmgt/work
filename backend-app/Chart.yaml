apiVersion: v2
name: backend-app
description: Chart Helm pour le backend de l'application 3-tier
type: application
version: 0.2.0
appVersion: "1.1"

dependencies:
  - name: cluster
    version: "0.6.0"       # Vérifiez la version disponible avec : helm search repo cnpg/cluster
    repository: "https://cloudnative-pg.github.io/charts"
    alias: postgresql